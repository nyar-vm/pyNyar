/// Fast but not safe
trait AbstructSet<T>: Iterable {
    def contains(self, key: T) -> bool;

    def get(self, key: K) -> bool;

    def insert(mut self, value: T) -> Optional<T>;

    def remove(mut self, value: T) -> Optional<T>;

    def count(self) -> Iterator<T> {
        self.iterator.count
    }

    def clear(self);

    def iterator(self) -> Iterator<T>;
}
